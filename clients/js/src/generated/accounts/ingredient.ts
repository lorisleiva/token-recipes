/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type Ingredient = Account<IngredientAccountData>;

export type IngredientAccountData = {
  key: Key;
  mint: PublicKey;
  recipe: PublicKey;
};

export type IngredientAccountDataArgs = {
  key: KeyArgs;
  mint: PublicKey;
  recipe: PublicKey;
};

/** @deprecated Use `getIngredientAccountDataSerializer()` without any argument instead. */
export function getIngredientAccountDataSerializer(
  _context: object
): Serializer<IngredientAccountDataArgs, IngredientAccountData>;
export function getIngredientAccountDataSerializer(): Serializer<
  IngredientAccountDataArgs,
  IngredientAccountData
>;
export function getIngredientAccountDataSerializer(
  _context: object = {}
): Serializer<IngredientAccountDataArgs, IngredientAccountData> {
  return struct<IngredientAccountData>(
    [
      ['key', getKeySerializer()],
      ['mint', publicKeySerializer()],
      ['recipe', publicKeySerializer()],
    ],
    { description: 'IngredientAccountData' }
  ) as Serializer<IngredientAccountDataArgs, IngredientAccountData>;
}

/** @deprecated Use `deserializeIngredient(rawAccount)` without any context instead. */
export function deserializeIngredient(
  context: object,
  rawAccount: RpcAccount
): Ingredient;
export function deserializeIngredient(rawAccount: RpcAccount): Ingredient;
export function deserializeIngredient(
  context: RpcAccount | object,
  rawAccount?: RpcAccount
): Ingredient {
  return deserializeAccount(
    rawAccount ?? (context as RpcAccount),
    getIngredientAccountDataSerializer()
  );
}

export async function fetchIngredient(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Ingredient> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'Ingredient');
  return deserializeIngredient(maybeAccount);
}

export async function safeFetchIngredient(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Ingredient | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeIngredient(maybeAccount) : null;
}

export async function fetchAllIngredient(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Ingredient[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'Ingredient');
    return deserializeIngredient(maybeAccount);
  });
}

export async function safeFetchAllIngredient(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Ingredient[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeIngredient(maybeAccount as RpcAccount));
}

export function getIngredientGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'tokenRecipes',
    '6EgVKvZu2V6cpZzarvDHuyeJwa1NB2ujj8hXY98pQpLE'
  );
  return gpaBuilder(context, programId)
    .registerFields<{ key: KeyArgs; mint: PublicKey; recipe: PublicKey }>({
      key: [0, getKeySerializer()],
      mint: [1, publicKeySerializer()],
      recipe: [33, publicKeySerializer()],
    })
    .deserializeUsing<Ingredient>((account) => deserializeIngredient(account));
}

export function getIngredientSize(): number {
  return 65;
}
