/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { findAssociatedTokenPda } from '@metaplex-foundation/mpl-toolbox';
import {
  AccountMeta,
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findWisdomFeaturePda } from '../accounts';
import { addAccountMeta, addObjectProperty } from '../shared';

// Accounts.
export type CollectExperienceInstructionAccounts = {
  /** The address of the recipe account */
  recipe: PublicKey | Pda;
  /** The authority of the recipe account */
  authority?: Signer;
  /** The wisdom feature PDA storing the valid experience mint */
  wisdomFeaturePda?: PublicKey | Pda;
  /** The mint account of experience tokens */
  experienceMint: PublicKey | Pda;
  /** The experience token account of the authority */
  experienceToken?: PublicKey | Pda;
};

// Data.
export type CollectExperienceInstructionData = { discriminator: number };

export type CollectExperienceInstructionDataArgs = {};

/** @deprecated Use `getCollectExperienceInstructionDataSerializer()` without any argument instead. */
export function getCollectExperienceInstructionDataSerializer(
  _context: object
): Serializer<
  CollectExperienceInstructionDataArgs,
  CollectExperienceInstructionData
>;
export function getCollectExperienceInstructionDataSerializer(): Serializer<
  CollectExperienceInstructionDataArgs,
  CollectExperienceInstructionData
>;
export function getCollectExperienceInstructionDataSerializer(
  _context: object = {}
): Serializer<
  CollectExperienceInstructionDataArgs,
  CollectExperienceInstructionData
> {
  return mapSerializer<
    CollectExperienceInstructionDataArgs,
    any,
    CollectExperienceInstructionData
  >(
    struct<CollectExperienceInstructionData>([['discriminator', u8()]], {
      description: 'CollectExperienceInstructionData',
    }),
    (value) => ({ ...value, discriminator: 11 })
  ) as Serializer<
    CollectExperienceInstructionDataArgs,
    CollectExperienceInstructionData
  >;
}

// Instruction.
export function collectExperience(
  context: Pick<Context, 'programs' | 'eddsa' | 'identity'>,
  input: CollectExperienceInstructionAccounts
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = context.programs.getPublicKey(
    'tokenRecipes',
    'C7zZZJpLzAehgidrbwdpBwN6RZCJo98qb55Zjep1a28T'
  );

  // Resolved inputs.
  const resolvedAccounts = {
    recipe: [input.recipe, true] as const,
    experienceMint: [input.experienceMint, true] as const,
  };
  addObjectProperty(
    resolvedAccounts,
    'authority',
    input.authority
      ? ([input.authority, false] as const)
      : ([context.identity, false] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'wisdomFeaturePda',
    input.wisdomFeaturePda
      ? ([input.wisdomFeaturePda, false] as const)
      : ([findWisdomFeaturePda(context), false] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'experienceToken',
    input.experienceToken
      ? ([input.experienceToken, true] as const)
      : ([
          findAssociatedTokenPda(context, {
            mint: publicKey(input.experienceMint, false),
            owner: publicKey(resolvedAccounts.authority[0], false),
          }),
          true,
        ] as const)
  );

  addAccountMeta(keys, signers, resolvedAccounts.recipe, false);
  addAccountMeta(keys, signers, resolvedAccounts.authority, false);
  addAccountMeta(keys, signers, resolvedAccounts.wisdomFeaturePda, false);
  addAccountMeta(keys, signers, resolvedAccounts.experienceMint, false);
  addAccountMeta(keys, signers, resolvedAccounts.experienceToken, false);

  // Data.
  const data = getCollectExperienceInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
